# view-task-plan

Read and follow `plugins/robos/skills/view-task-plan/SKILL.md` in the configured RobOS checkout. Pass through the user's selector and requested changes; use an explicit external graph root.
