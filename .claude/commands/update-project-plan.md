# update-project-plan

Read and follow `plugins/robos/skills/update-project-plan/SKILL.md` in the configured RobOS checkout. Pass through the user's selector and requested changes; use an explicit external graph root.
